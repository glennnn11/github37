apiVersion: apps/v1
kind: Deployment
metadata:
  name: glenn-nginx-deployment
  namespace: glenn-eks-activity
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      serviceAccountName: glenn-nginx-service-account
      containers:
      - name: nginx
        image: nginx:latest
        ports:
        - containerPort: 80