apiVersion: v1
kind: Service
metadata:
  name: glenn-nginx-service
  namespace: glenn-eks-activity
spec:
  type: LoadBalancer
  selector:
    app: nginx
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80